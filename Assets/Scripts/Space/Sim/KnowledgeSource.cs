namespace OuterSpace.Sim
{
    /// <summary>
    /// Откуда кораблю известно об объекте. Прибор показывает не мир, а знание корабля
    /// о мире; пока источник у всех Database и ничего не фильтруется, но проставляется
    /// он сразу - иначе к радиоразведке пришлось бы возвращаться и расставлять его
    /// задним числом по всему коду, забыв половину мест.
    /// </summary>
    public enum KnowledgeSource
    {
        Unknown,
        Database,
        Transponder,
        Bearing,
        Visual,
    }
}
